<IfModule mod_rewrite.c>
    RewriteEngine On
    # Redirect all requests not leading to an existing file or directory to index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule (.*) index.php?$1 [QSA,L]
</IfModule>